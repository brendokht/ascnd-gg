generator client {
  provider = "prisma-client"
  output   = "../../src/generated/prisma"
  moduleFormat = "cjs"
  importFileExtension = ""
}

generator dbml {
  provider = "prisma-dbml-generator"
}

datasource db {
  provider = "postgresql"
}
