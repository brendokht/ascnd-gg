/** @type {import('prettier').Config} */
const config = {
  semi: true,
  arrowParens: "always",
  singleQuote: false,
  trailingComma: "all",
  plugins: ["prettier-plugin-tailwindcss"],
};

export default config;
